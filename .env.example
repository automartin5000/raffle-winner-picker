# =============================================================================
# RAFFLE WINNER PICKER - ENVIRONMENT VARIABLES
# =============================================================================

# -----------------------------------------------------------------------------
# AUTH0 CONFIGURATION
# -----------------------------------------------------------------------------

# Your Auth0 tenant domain (e.g., 'your-tenant.auth0.com')
# TODO: Replace with your actual Auth0 domain
AUTH0_DOMAIN=your-tenant.auth0.com

# Management API Application Credentials (Machine to Machine)
# These are used to create/manage SPA clients via the Management API
# TODO: Create a Machine-to-Machine application in Auth0 and get these credentials
AUTH0_CLIENT_ID=your-management-api-client-id
AUTH0_CLIENT_SECRET=your-management-api-client-secret

# SPA Callback URL - Optional manual override (auto-derived from hosted zones)
# For local development: http://localhost:5173
# If not set, will be derived from PROD_HOSTED_ZONE (prod) or NONPROD_HOSTED_ZONE (dev)
AUTH0_SPA_CALLBACK_URL=http://localhost:5173

# Auth0 audience for API access (optional, for future API integration)
VITE_AUTH0_AUDIENCE=

# -----------------------------------------------------------------------------
# FRONTEND BUILD VARIABLES (Auto-generated)
# -----------------------------------------------------------------------------

# These will be populated automatically by the Auth0 client management script
VITE_AUTH0_CLIENT_ID=
VITE_AUTH0_CLIENT_ID_DEV=
VITE_AUTH0_CLIENT_ID_PROD=

# Deployment metadata (auto-generated)
VITE_DEPLOY_ENV=dev
VITE_DEPLOY_EPHEMERAL=false

# Auth0 domain for frontend (usually same as AUTH0_DOMAIN)
VITE_AUTH0_DOMAIN=your-tenant.auth0.com

# Frontend Environment Variables (auto-generated)
# These will be populated by the Auth0 client management script
VITE_PROD_HOSTED_ZONE=
VITE_NONPROD_HOSTED_ZONE=

# -----------------------------------------------------------------------------
# AWS DEPLOYMENT VARIABLES (For CDK/Infrastructure)
# -----------------------------------------------------------------------------

# AWS Account IDs
# TODO: Set these with your actual AWS account IDs
NONPROD_AWS_ACCOUNT_ID=123456789012
PROD_AWS_ACCOUNT_ID=123456789012

# Hosted Zone Domain Names for Route 53
# TODO: Set these with your actual domain names (keep private for security)
# Example patterns: dev.yourapp.com and yourapp.com
NONPROD_HOSTED_ZONE=your-dev-domain.com
PROD_HOSTED_ZONE=your-prod-domain.com

# Deployment environment identifier
DEPLOY_ENV=local

# Whether this is an ephemeral (PR-based) deployment
DEPLOY_EPHEMERAL=true
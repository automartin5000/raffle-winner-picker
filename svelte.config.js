import adapter from '@sveltejs/adapter-static';
import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';

/** @type {import('@sveltejs/kit').Config} */
const config = {
    // Ref: https://svelte.dev/docs/kit/integrations#vitePreprocess
	preprocess: vitePreprocess(),
    // Ref: https://svelte.dev/docs/kit/adapter-static
	kit: {
		adapter: adapter({
			pages: 'build',
			assets: 'build',
			fallback: undefined,
			precompress: false,
			strict: true
		})
	}
};

export default config;
